git pull
git add .
git commit -m "%*"
git push

@REM Im Terminal muss dann nur noch die ".\up.bat %*" Datei ausgeführt werden! "%*" ist eine Variable für den commit-Kommentar, den man dann auf GitHub hinterlässt!